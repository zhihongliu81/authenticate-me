import {useHistory} from 'react-router-dom';


const RequestAttendee = (event, user) => {
    // const history = useHistory()
    // console.log(user);
    // if (!event) return null;
    // if (!user) return history.push('/signup');
    return null;
}


export default RequestAttendee;
